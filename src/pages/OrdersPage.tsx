import { MyPage } from '@/components/MyPage';
import { SectionContainer } from '@/components/SectionContainer';

export default function OrdersPage() {
  return (
    <MyPage>
      <SectionContainer>This is orders page.</SectionContainer>
    </MyPage>
  );
}
